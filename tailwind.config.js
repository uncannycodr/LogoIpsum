/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [],
  theme: {
    extend: {
      colors: {
        secondary: "var(--secondary)",
        primary: "var(--primary)",
        "card-bg": "var(--card-bg)",
        gray: "var(--gray)",
        dot: "var(--dot)",
        darkGray: "var(--darkGray)",
      },
    },
  },
  plugins: [],
};
